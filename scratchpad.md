workflow-init --uc --ultrathink --all-mcp --persona-architect --analyze --verbose "i need for you to invoke the error-eliminator so he can summon his team of specialists to fix my codebase"

```bash
/ultra-think  --uc --ultrathink --parsona-architect --deep-dive --seq "i need for you to perform a deep analysis to identify missing components (e.g., MFA, session management), dependencies, logic flows, and foundational
  services for the "crews" feature. From when a user creates a crew and creating thea new
```

```bash
  claude-flow@alpha hive-mind "Invoke @error-eliminator.md and have that agent unleash it's team of elite problem solvers to analyze and identify every possible error in the @lib directory." --auto-spawn --queen-type tactical
  --max-workers 10 --claude
```

```bash
   npx claude-flow@alpha hive-mind "Invoke 10 agents to completed the tasks in archon. after each task has been complete, call Jenny and Keren to validadte the completion of the task." --auto-spawn --queen-type
  tactical --max-workers 10 --claude
```

```bash
/superclaude:analyze --uc --ultrathink --persona-architect --seq --depth-deep I need a comprehensive and detailed report formatted in markdown. With each identification, in the report provide a copy of the widget containing
  the error,  the domain  the error is
    related too, as well as any other criteria that i may have overlooked that could be benifitial information for the task-orchestrator.
```

```bash
/sparc:coder --persona-frontend --magic "We need to improve or redesign the cards on @lib\screens\locals\locals_screen.dart [Pasted text #1 +33 lines]I really liked the old layout of the card.  Basically duplicating the
  @lib\widgets\rich_text_job_card.dart utilizing the rich text widget to effectively display the locals data. I need for you to revert these cards back to the rich tect cards"
```

```bash
  > /ultra-think  --uc --ultrathink --parsona-architect --deep-dive --seq "i need for you to perform a deep analysis to identify missing components (e.g., MFA, session management), dependencies, logic flows, and foundational
  services for the "crews" feature. From when a user creates a crew and creating thea new document in the "crews" colllection, creating a sub-colection for setting that crews job preferences, inviting new members, directly
  chatting and sending messages to other members if that crew, posting messages in the `feed` tab for all users to interact with regardless if they are members of any crew or not, the supporting services, models, and providers,  
  EVERYTHING. Generate a technical analysis document specifying exactly what's missing and a detailed implementation guide on how to integrate them to restore full functionality."
```

```bash
   /agent-task-prep --uc --ultrathink ---all-mcp --persona-architect --iterations 5 --plan --feature
    --technical "I need for you to read @.claude\skills\task-orchestrator\SKILL.md and utilize the advanced task generation to generate comprehensive detailed tasks from
  @docs\technical_analysis\crews_feature_analysis_and_implementation_guide.md after generating sufficent and appropriate amount of tasks that will correctly and completely meet the suggested corrections without making the tasks  
  overly complicated to eitherunderstand the purpose of the task nor how to implement the task."
```

```bash
  > npx claude-flow@alpha hive-mind spawn --uc --ultrathink --all-mcp "I need for you to spawn advanced and specialized agents towork in parralel to work out all of the tasks i @hierarchical-initialization-tasks.md once the
  agents have completed all of thier assigned tasks invoke the agent karen to review the newly implemented code corrections and or integrations to ensure that the code is fully functional and operational." --queen-type
  strategic --max-workers 8 --claude
```

```bash
   /ultra-think  --uc --ultrathink --all-mcp Task-Expert: Convert the @ANALYSIS_REPORT.md into a detailed master task list using Task Generation SKILL framework. Generate hierarchical-initialization-tasks.md with atomic,
  sequenced, agent-assigned tasks ready for execution.
```

```bash
  > /sparc:documenter --uc --ultrathink --example --persona-mentor --c7 --seq "Read @error-correction-workflow\E2E_COMPREHENSIVE_WORKFLOW_GUIDE.md I need for you to break this very large and complex workflow guide into 6 smaller
  manageble parts. Part 1. Codebase analysis Part 2. Analysis Validation Part 3. Task Creation Part 4. Task Validation  Part 5. Task Execution Part 6. Task Completion Validation.  Reference
  @error-correction-workflow\E2E_COMPREHENSIVE_WORKFLOW_GUIDE.md exclusively. DO NOT REFERENCE, INCLUDE, OR MODIFY ANYTHING IN THESE 6 SMALLER SECTIONS FROM THE MASTER GUIDE. ALSO, BE SURE TO INCLUDE EVERYTHING THAT IS DEFINED
  IN @error-correction-workflow\E2E_COMPREHENSIVE_WORKFLOW_GUIDE.md
```

```bash
 /sparc:swarm-coordinator --uc --ultrathink --persona-frontend --all-mcp --agent-ui-comprehensive-tester --agent-ui-designer --agent-ui-ux-designer --agent-ux-designer --agent-flutter-expert --agent-ui-engineer --wave-delegation-files --wave-strategy-adaptive --wave-mode force  "i am only concerned with the errors in the \lib directory. I want to work directory by directory correcting errors. lets start with the model and provider directories. 
```

```bash
/document  --c7 --seq --uc --ultrathink  --persona-mentor --aagent-docs-architect --agent-documentation-manager --agent-code-reviewer-pro --agent-product-manager"read @lib\features\CLAUDE.md to understand your role in this directory as well as how to use the archon mcp server tools. What i need for you to do is to spawn code reviewer pro to help the docs architect and documentation manager agents analyze and understandand the exact architecture of the @tailboard_screen.dart file. While the product-manager agent uses the archon mcp server tools to understand exactly what is required to build out the chatting feature, like the channel list, group(crew) messaging, user profiles, etc. The core and fundamental foundational blocks. One the product manager has a good understanding of what is required, that agent will colaborate with the others to figure out how to implement evrything using the existing scaffolding utilizing popup, overlays, the four containers in the row above the `tab-bar`. This will be a very complex and detail oriendted workflow. So be sure to take your time, plan your work, and work your plan."
```

---

```dart
/document  --c7 --seq --uc --ultrathink  --persona-mentor --aagent-docs-architect --agent-documentation-manager --agent-code-reviewer-pro --agent-product-manager"read @lib\features\CLAUDE.md to understand your role in this directory as well as how to use the archon mcp server tools. What i need for you to do is to spawn code reviewer pro to help the docs architect and documentation manager agents analyze and understandand the exact architecture of the @tailboard_screen.dart file. While the product-manager agent uses the archon mcp server tools to understand exactly what is required to build out the chatting feature, like the channel list, group(crew) messaging, user profiles, etc. The core and fundamental foundational blocks. One the product manager has a good understanding of what is required, that agent will colaborate with the others to figure out how to implement evrything using the existing scaffolding utilizing popup, overlays, the four containers in the row above the `tab-bar`. This will be a very complex and detail oriendted workflow. So be sure to take your time, plan your work, and work your plan."
```

```dart
/analyze @lib/ @src/ --ultrathink --seq --c7 --persona-analyzer \
  --code --deps --architecture --forensic --dry-run --evidence \
  --introspect --uc > merge-plan.md
```

```dart
/improve @merge-plan.md --quality --refactor --batch \
  --persona-refactorer --iterate --force --validate \
  --git-checkpoint "pre-unification" --parallel --uc --watch \
  > unification-report.md
```

```dart
/cleanup @src/ @lib/ --code --aggressive --persona-analyzer \
  --seq --deps --validate --coverage --introspect \
  --git-checkpoint "pre-callgraph-strip" --uc \
  > callgraph-cleanup.md
```

```dart
/design @src/state/ @src/providers/ --architecture --simplify \
  --persona-architect --ultrathink --seq --refactor --validate \
  --interactive --git-checkpoint "pre-state-collapse" --uc

/improve @state-collapse-plan.md --quality --iterate \
  --threshold 95% --force --validate
```

```dart
/design @src/ --performance --architecture --persona-performance \
  --ultrathink --seq --pup --cache --load-balance --introspect \
  --git-checkpoint "pre-perf-rearch" --uc

/build @performance-architecture.md --feature --tdd \
  --validate --benchmark
```

```dart
/cleanup @assets/ @node_modules/ @package.json --deps --files \
  --assets --aggressive --persona-performance --validate \
  --bundle-analyze --tree-shake --minify \
  --git-checkpoint "pre-compression" --uc
```

```dart
/scan @src/ @lib/ @tests/ --quality --complexity --performance \
  --forensic --persona-analyzer --all-mcp --seq --c7 --pup \
  --evidence --strict --coverage --introspect --uc
```

```dart
/test @src/ @lib/ --coverage --e2e --integration --unit \
  --mutation --performance --accessibility --persona-qa --pup \
  --strict --threshold 95% --uc

/document @src/ @lib/ @README.md --technical --user --api \
  --interactive --persona-scribe --c7 --examples --visual --uc

/git --checkpoint "40-percent-reduction-complete" \
  --tag "v2.0.0-optimized" --validate --test --pre-commit

/scan --security --owasp --compliance --quality --deps \
  --persona-security --strict --validate
```

---
---

```bash
/hive   --uc --ultrathink --parsona-frontend --deep-dive --seq --e2e --magic --seq --focus frontend  --wave-delegation folders --wave-strategy systematic adaptive --wave-mode force --concurrency 3 --delegate auto --introspection --interactive --scope 2 --focus-frontend --iterations 3